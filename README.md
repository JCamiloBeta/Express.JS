# Express.JS

Para que se cargue la configuración inicial del Proyecto, ejecutar en la carpeta raíz del proyecto en la Terminal la siguiente instrucción: 

`npm i nodemon eslint eslint-config-prettier eslint-plugin-prettier prettier -D`
